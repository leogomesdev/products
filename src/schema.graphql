type Product {
  id: ID!,
  name: String!
}

type Query {
  products: [Product]!
}

type Mutation {
  createProduct(name: String!): Product!,
  updateProduct(id: ID!, name: String!): Product!
  deleteProduct(id: ID!): String!
}
